# המשך מכאן — 10.9.2026

## מצב

תיקונים נכתבו לקוד המקומי אחרי ניתוח 31 שיחות אמיתיות מהשבוע.
**הם לא נבדקו ולא נדחפו.** סביבת הלינוקס נפלה באמצע, ולכן 151 הבדיקות לא רצו.

## הפעולה הראשונה בשיחה החדשה

```
cd "C:\claude folder\whatsapp-passport-bot"
node tests/scenarios.test.js
```

אמורות לעבור 151. אם משהו נכשל — התיקונים למטה הם החשודים.

ואז, רק אם הכל ירוק:

```
git add -A
git commit -m "Fix geography, route attributes, media bursts, English placeholders"
git push origin master
```

---

## מה תוקן (לא בדוק)

| קובץ | תיקון |
|---|---|
| `src/ai/prompts.js` | רשימת ערים מפורשת — מה סעיף 11 ומה נמצא היום ברומניה |
| `src/ai/prompts.js` | איסור לערבב תכונות בין מסלולים ("אין מגבלת דורות" = הסדרה בלבד) |
| `src/ai/prompts.js` | כתובות המשרד נעולות · פטור B1 מגיל 65 · איסור להמציא סטטוס תפעולי |
| `src/ai/prompts.js` | `ancestor` חייב לחזור בעברית |
| `src/sales/leadProfile.js` | הוספת רומן, וישו דה סוס, פיאטרה ניאמץ, דורוחוי ועוד לטבלת הערים |
| `src/sales/nextAction.js` | `normaliseAncestor` מתרגם ערכים באנגלית לפני שהם נאמרים |
| `src/sales/closing.js` | "במשך היום", "אחהצ", "בקר" נחשבים מועד שסוכם |
| `src/flows.js` | חלון שקט של 45 שניות אחרי מסמך — כדי לא לענות שש פעמים על חמש תמונות |

---

## מה שנשאר לתקן

**איסוף טלפון כשיש שם בלבד.** שמשון לזר ומרקוביץ ליאת מסרו שם, והבוט לא ביקש
מספר. שני לידים אבודים.

**הליד שאבד עם ניקוד 100.** מספר +972528906975 — תיק של ארבעה אנשים, שאל על
עלות, קיבל תשובה ואז הצעת מייל מנותקת. לא נאסף טלפון.

**Tal Yehoshua.** הבוט הבטיח "דקות ספורות", והלקוח חיכה 35 דקות עד שהדדליין
שלו עבר. צריך כלל: אל תבטיח זמן תגובה שאינך שולט בו.

**דף סיכום שבועי.** הבעלים ביקש לראות את שטף השיחות השבועי. קיים רק
`/admin/transcripts?days=7`; דף עם ספירות יומיות והמרות עוד לא נבנה.

---

## נתוני הבסיס מהשבוע

31 שיחות · 10 הסתיימו עם שם וטלפון (**32% המרה**) ·
**10 מתו אחרי ההודעה הראשונה** — הלקוח לחץ על המודעה, קיבל את השאלה, ולא ענה.

זו הנקודה עם המנוף הגדול ביותר: שליש מהתקציב הפרסומי נעצר בשאלה הראשונה.
